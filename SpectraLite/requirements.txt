# SpectraLite — Phase 0 dependencies (local / non-Colab)
# Pin torch>=2.2 for torch.utils.flop_counter.FlopCounterMode (later phases).
#
# On Google Colab: do NOT pip-install torch from this file.
# Colab already ships a CUDA-enabled PyTorch; reinstalling often
# replaces it with a CPU-only wheel and breaks GPU. Use
# requirements-colab.txt (or the Phase0 notebook install cell) instead.

torch>=2.2
transformers>=4.40.0
accelerate>=0.30.0
datasets>=2.19.0
evaluate>=0.4.0
lm-eval>=0.4.0
calflops>=0.3.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.8.0
tqdm>=4.66.0
